const TEACHER_CODE = 'Abdelrahman3177';
const INSTA_PAY = '01014812293';
const DB_KEY = 'english-zone-db';
const SESSION_KEY = 'english-zone-session';

const seed = {
  students: [{ id:'s1', name:'Maya Hassan', phone:'01000000000', email:'maya@example.com', grade:'Secondary 2', course:'English Foundations', parentPhone:'01011111111', password:'maya123', code:'EZ-1048', status:'Active', payment:'Approved', joined:'2026-09-01' }],
  courses: [
    { id:'c1', name:'English Foundations', level:'Secondary 2', description:'Build strong grammar, vocabulary, and everyday communication skills.', price:650, status:'Active', progress:72, students:12 },
    { id:'c2', name:'Academic Writing Lab', level:'Secondary 3', description:'Write clearly, organize ideas, and prepare for school examinations.', price:750, status:'Active', progress:38, students:8 },
    { id:'c3', name:'Conversation Club', level:'All levels', description:'Practice confident speaking through friendly weekly challenges.', price:450, status:'Active', progress:0, students:16 }
  ],
  exams: [{ id:'e1', title:'Grammar Checkpoint 01', course:'English Foundations', date:'2026-09-18', duration:'45 min', status:'Available', score:null }],
  grades: [{ student:'s1', exam:'Vocabulary Checkpoint', course:'English Foundations', score:88, total:100, date:'2026-09-04', comment:'Great progress. Keep reviewing collocations.' }],
  attendance: [{ student:'s1', date:'2026-09-05', course:'English Foundations', status:'Present', note:'Ready and engaged.' }, { student:'s1', date:'2026-09-02', course:'English Foundations', status:'Present', note:'' }],
  payments: [{ id:'p1', student:'s1', course:'English Foundations', amount:650, transaction:'IP-88319', sender:'01000000000', date:'2026-09-02', status:'Approved', reason:'' }],
  announcements: [{ id:'a1', title:'Welcome to the new term', audience:'All students', date:'2026-09-01', body:'Your weekly learning plan is now available.' }],
  notifications: [{ text:'Your payment for English Foundations was approved.', date:'Today' }, { text:'Grammar Checkpoint 01 is available on September 18.', date:'Yesterday' }]
};

const clone = value => JSON.parse(JSON.stringify(value));
let db = JSON.parse(localStorage.getItem(DB_KEY) || 'null') || clone(seed);
// A browser refresh must never grant dashboard access from stale client data.
// The server cookie is established only by a successful login form submission.
let session = null;
let teacherExamsHydrated = false;
let teacherPaymentsHydrated = false;
localStorage.removeItem(SESSION_KEY);
const app = document.querySelector('#app');
const esc = value => String(value ?? '').replace(/[&<>'"]/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const save = () => localStorage.setItem(DB_KEY, JSON.stringify(db));
const currentStudent = () => session?.role === 'student' ? db.students.find(item => item.id === session.id) : null;
const route = () => (location.hash.replace('#/', '').split('#')[0]) || 'home';
const navigate = path => { location.hash = `#/${path}`; };
const icon = letter => `<span class="side-icon">${letter}</span>`;
const initials = name => name.split(' ').map(part => part[0]).slice(0,2).join('').toUpperCase();
const toast = (message, type='success') => { const node = document.createElement('div'); node.className = `toast ${type}`; node.innerHTML = `<strong>${type === 'success' ? '✓' : '!'}</strong><span>${esc(message)}</span>`; document.querySelector('#toast-region').append(node); setTimeout(() => node.remove(), 3400); };
const confirmAction = (title, message, onConfirm) => { document.querySelector('#modal-region').innerHTML = `<div class="modal-backdrop"><div class="modal animate-in"><h3>${esc(title)}</h3><p>${esc(message)}</p><div class="modal-actions"><button class="btn btn-plain" data-close-modal>Cancel</button><button class="btn btn-danger" data-confirm-modal>Confirm</button></div></div></div>`; document.querySelector('[data-confirm-modal]').onclick = () => { onConfirm(); document.querySelector('#modal-region').innerHTML=''; }; document.querySelector('[data-close-modal]').onclick = () => document.querySelector('#modal-region').innerHTML=''; };

const logo = `<span class="brand-mark" aria-hidden="true"><span class="brand-glyph">✎</span></span><span>English Zone</span>`;
function publicNav() { return `<header class="topbar"><div class="container" style="display:flex;align-items:center;justify-content:space-between;width:100%"><a class="brand" href="#/home">${logo}</a><nav class="nav"><a href="#/home">Home</a><a href="#/home#courses">Courses</a><a href="#/home#about">About</a><a href="#/home#contact">Contact</a></nav><div class="actions"><a class="btn btn-plain btn-small" href="#/login">Student Login</a><a class="btn btn-soft btn-small" href="#/teacher-login">Teacher Login</a><a class="btn btn-primary btn-small" href="#/register">Create Account</a><button class="mobile-menu" data-menu>☰</button></div></div></header>`; }

function homePage() { return `<div class="app-shell home-page">${publicNav()}<main>
  <section class="hero reference-hero"><div class="reference-shape corner-top-left"></div><div class="reference-shape corner-top-right"></div><div class="reference-shape corner-bottom-left"></div><div class="reference-shape corner-bottom-right"></div><div class="reference-dots dots-left"></div><div class="reference-dots dots-right"></div><div class="reference-squiggle squiggle-left">⌁</div><div class="reference-squiggle squiggle-right">⌁</div><div class="reference-star star-left">☆</div><div class="reference-star star-right">☆</div><div class="reference-pencil">✎</div><div class="reference-plane plane-left">➤</div><div class="reference-plane plane-right">➤</div><div class="container hero-grid"><div class="animate-in hero-copy"><div class="welcome-script">Welcome to <span>⌁</span></div><h1 class="reference-title">ENGLISH ZONE</h1><div class="reference-motto"><span>Learn</span><b>•</b><span>Practice</span><b>•</b><span>Improve</span></div><p>Grow your confidence with structured lessons, practical exams, and steady support from Mr. Abdulrahman Mohammed.</p><div class="hero-actions"><a class="btn btn-primary" href="#/register">Start Learning <span>→</span></a><a class="btn btn-soft" href="#/home#courses">Explore Courses</a></div><div class="hero-proof"><span class="proof-avatar">AM</span><span><strong>Teacher-led progress</strong><small>Clear lessons. Confident results.</small></span></div></div><div class="hero-visual"><div class="reference-blob"></div><div class="reference-ring ring-one"></div><div class="reference-ring ring-two"></div><div class="reference-tower"><span class="tower-roof">◆</span><span class="tower-clock">◷</span><span class="tower-body">▥</span></div><div class="speech-bubble">Let's<br>Learn<br>English</div><div class="reference-book-stack"><span>Grammar</span><span>Vocabulary</span><span>English</span></div><div class="reference-notebook"><b>✓ Speak</b><b>✓ Read</b><b>✓ Write</b><b>✓ Grow</b></div><div class="reference-pencils">▌▌</div><div class="reference-letter letter-a">A</div><div class="reference-letter letter-b">B</div><div class="reference-letter letter-c">C</div></div></div></section>
  <section class="stats-strip"><div class="container stats-grid"><div class="stat-tile"><div class="icon-box">▦</div><div><strong>Structured Courses</strong><small>Learn with a clear path</small></div></div><div class="stat-tile"><div class="icon-box lav">✦</div><div><strong>Online Exams</strong><small>Practice with purpose</small></div></div><div class="stat-tile"><div class="icon-box">↗</div><div><strong>Student Progress</strong><small>See every milestone</small></div></div><div class="stat-tile"><div class="icon-box lav">✎</div><div><strong>Teacher Support</strong><small>Guidance when it matters</small></div></div></div></section>
  <section class="feature-section"><div class="container"><div class="section-head"><div><span class="eyebrow">Everything in one place</span><h2 style="margin-top:14px">Everything You Need to Learn Better</h2><p>Thoughtful tools for a focused, confident learning routine.</p></div></div><div class="feature-grid stagger"><div class="feature-card"><div class="icon-box">▦</div><h3>Organized Courses</h3><p>Clear learning paths built around your level and goals.</p></div><div class="feature-card lavender"><div class="icon-box lav">✦</div><h3>Online Exams</h3><p>Practice, submit, and understand your results in one place.</p></div><div class="feature-card"><div class="icon-box">↗</div><h3>Grades Tracking</h3><p>See your improvement over time with simple progress signals.</p></div><div class="feature-card lavender"><div class="icon-box lav">◷</div><h3>Attendance Tracking</h3><p>Build consistency and keep your learning routine visible.</p></div><div class="feature-card"><div class="icon-box">₱</div><h3>Easy Payments</h3><p>Submit payment details and follow every review status.</p></div><div class="feature-card lavender"><div class="icon-box lav">✎</div><h3>Teacher Support</h3><p>Get thoughtful guidance throughout your English journey.</p></div></div></div></section>
  <section class="section courses-section" id="courses"><div class="container"><div class="section-head"><div><span class="eyebrow">Choose your path</span><h2 style="margin-top:14px">Explore Our Courses</h2><p>Simple structure, useful practice, and a pace you can trust.</p></div><a class="btn btn-soft" href="#/register">View all courses</a></div><div class="courses-grid">${db.courses.map(courseCard).join('')}</div></div></section>
  <section class="section about-section" id="about"><div class="container"><div class="teacher-band"><div class="teacher-badge"><span>✎</span><small>Learn</small></div><div class="teacher-copy"><span class="eyebrow">Meet Your Teacher</span><h2 style="margin-top:14px">Mr. Abdulrahman Mohammed</h2><p>A patient, structured approach to English learning: understand the idea, practice it, then use it with confidence. Every lesson is designed to make your next step clear.</p><div class="teacher-points"><span>✦ Practical lessons</span><span>✓ Personal feedback</span><span>↗ Visible progress</span></div></div><a class="btn btn-primary" href="#/register">Learn with me →</a></div></div></section>
  <section class="cta" id="contact"><span class="eyebrow">Your next chapter starts here</span><h2 style="margin-top:16px">Ready to Improve Your English?</h2><p>Join a focused learning space built around your progress.</p><div class="hero-actions"><a class="btn btn-primary" href="#/register">Create Student Account</a><a class="btn btn-soft" href="#/home#courses">Explore Courses</a></div></section>
  </main><footer class="footer container"><div><a class="brand" href="#/home">${logo}</a><small>Learn with clarity. Grow with confidence.</small></div><nav><a href="#/home">Home</a><a href="#/home#courses">Courses</a><a href="#/home#about">About</a><a href="#/home#contact">Contact</a><a href="#/login">Student Login</a><a href="#/teacher-login">Teacher Login</a></nav><span>© 2026 English Zone</span></footer></div>`; }
function courseCard(course) { return `<article class="course-card"><div class="course-top"><span class="pill green">${esc(course.status)}</span><span class="pill">${esc(course.level)}</span></div><h3>${esc(course.name)}</h3><p>${esc(course.description)}</p><small style="display:block;color:var(--muted);margin-top:14px">Teacher · Mr. Abdulrahman Mohammed</small><strong class="course-price">${course.price} EGP <small style="font:400 12px DM Sans;color:var(--muted)">/ course</small></strong><a class="btn btn-soft" href="#/register">View Course →</a></article>`; }

function authPage(mode='student') { const teacher = mode === 'teacher'; return `<div class="auth-page"><div class="auth-card animate-in"><a class="brand" href="#/home">${logo}</a><h1>${teacher ? 'Teacher access' : 'Welcome Back'}</h1><p>${teacher ? 'Use your teacher code to manage the learning space.' : 'Sign in to continue your English learning journey.'}</p>${teacher ? `<form data-form="teacher-login"><div class="field"><label>Teacher Code</label><input name="code" placeholder="Enter your teacher code" required /></div><div data-form-error></div><button class="btn btn-primary" type="submit">Open Teacher Dashboard →</button></form>` : `<div class="tabs"><button class="tab active" data-auth-tab="code">Student Code</button><button class="tab" data-auth-tab="data">Personal Data</button></div><div data-auth-panel="code"><form data-form="student-code"><div class="field"><label>Student Code</label><input name="code" placeholder="Example: EZ-1048" required /></div><div data-form-error></div><button class="btn btn-primary" type="submit">Login to Platform →</button></form></div><div data-auth-panel="data" style="display:none"><form data-form="student-data"><div class="form-grid"><div class="field full"><label>Email Address</label><input name="email" type="email" required /></div><div class="field"><label>Password</label><input name="password" type="password" required /></div><div class="field"><label>Full Name</label><input name="name" required /></div></div><div data-form-error></div><button class="btn btn-primary" type="submit">Login →</button></form></div><div class="helper">New to English Zone? <button data-go="register">Create New Account</button></div>`}<div class="helper"><button data-go="home">← Back to home</button></div></div></div>`; }
function registerPage() { return `<div class="auth-page"><div class="auth-card animate-in" style="width:min(700px,100%)"><a class="brand" href="#/home">${logo}</a><h1>Create your account</h1><p>Set up your learning profile and take the first step.</p><form data-form="register"><div class="form-grid"><div class="field"><label>Full Name</label><input name="name" required /></div><div class="field"><label>Phone Number</label><input name="phone" required /></div><div class="field"><label>Email Address</label><input type="email" name="email" required /></div><div class="field"><label>Grade or School Level</label><select name="grade"><option>Primary 6</option><option>Preparatory 1</option><option>Preparatory 2</option><option>Secondary 1</option><option>Secondary 2</option><option>Secondary 3</option></select></div><div class="field full"><label>Selected Course</label><select name="course">${db.courses.map(c=>`<option>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>Parent or Guardian Phone</label><input name="parentPhone" required /></div><div class="field"><label>Registration Date</label><input type="date" name="joined" value="${new Date().toISOString().slice(0,10)}" required /></div><div class="field"><label>Password</label><input type="password" name="password" minlength="6" required /></div><div class="field"><label>Confirm Password</label><input type="password" name="confirm" minlength="6" required /></div></div><div data-form-error></div><button class="btn btn-primary" type="submit">Create Student Account →</button></form><div class="helper">Already have an account? <button data-go="login">Student Login</button></div></div></div>`; }

const studentLinks=[['student-dashboard','D','Dashboard'],['courses','C','My Courses'],['exams','E','Exams'],['grades','G','Grades'],['attendance','A','Attendance'],['payments','P','Payments'],['my-code','K','My Code'],['profile','U','Profile'],['settings','S','Settings']];
const teacherLinks=[['teacher-dashboard','D','Dashboard'],['students','S','Students'],['teacher-courses','C','Courses'],['teacher-exams','E','Exams'],['teacher-grades','G','Grades'],['teacher-attendance','A','Attendance'],['teacher-payments','P','Payments'],['announcements','N','Announcements'],['settings','S','Settings']];
function dashboardShell(kind, content) { const teacher=kind==='teacher'; const links=teacher?teacherLinks:studentLinks; const user=teacher?'Mr. Abdulrahman Mohammed':currentStudent()?.name || 'Student'; return `<div class="dashboard"><aside class="sidebar" data-sidebar><a class="brand" href="#/${teacher?'teacher-dashboard':'student-dashboard'}">${logo}</a><nav class="side-nav">${links.map(([path,letter,label])=>`<a class="side-link ${route()===path?'active':''}" href="#/${path}">${icon(letter)}${label}</a>`).join('')}</nav><div class="sidebar-bottom"><a class="side-link" href="#/home" data-logout>${icon('↪')}Logout</a></div></aside><div class="dash-main"><header class="dash-top"><div style="display:flex;align-items:center;gap:12px"><button class="mobile-menu" data-sidebar-toggle>☰</button><h2>${pageTitle(route())}</h2></div><div class="top-user"><span style="color:var(--muted);font-size:13px">Notifications&nbsp; ◦</span><span class="avatar">${initials(user)}</span><strong style="font-size:13px">${esc(user)}</strong></div></header><main class="dash-content animate-in">${content}</main></div></div>`; }
function pageTitle(path){ return ({'student-dashboard':'Dashboard',courses:'My Courses',exams:'Exams',grades:'Grades',attendance:'Attendance',payments:'Payments','my-code':'My Code',profile:'Profile',settings:'Settings','teacher-dashboard':'Dashboard',students:'Students','teacher-courses':'Courses','teacher-exams':'Exams','teacher-grades':'Grades','teacher-attendance':'Attendance','teacher-payments':'Payments',announcements:'Announcements'})[path] || 'Dashboard'; }
function studentDashboard(){ const student=currentStudent(), course=db.courses.find(c=>c.name===student?.course)||db.courses[0], grades=db.grades.filter(g=>g.student===student?.id), notices=db.notifications; return dashboardShell('student',`<div class="dash-welcome"><div><h1 style="font-size:32px;letter-spacing:-1px">Good morning, ${esc(student?.name.split(' ')[0])}.</h1><p>Here is your learning snapshot for today.</p></div><a class="btn btn-primary" href="#/courses">Continue learning →</a></div><div class="stat-grid"><div class="stat-card"><span class="stat-label">Student Code</span><strong style="font-size:21px">${student?.code}</strong><small>Active code</small></div><div class="stat-card"><span class="stat-label">My Courses</span><strong>1</strong><small>1 active course</small></div><div class="stat-card"><span class="stat-label">Course Progress</span><strong>${course.progress}%</strong><small>+8% this month</small></div><div class="stat-card"><span class="stat-label">Attendance</span><strong>100%</strong><small>Excellent consistency</small></div><div class="stat-card"><span class="stat-label">Payment Status</span><strong style="font-size:20px;color:var(--green)">${student?.payment}</strong><small>Up to date</small></div></div><div class="dash-grid"><section class="panel"><div class="panel-head"><h3>Learning progress</h3><a href="#/courses">View course</a></div><div class="progress-row"><div class="progress-meta"><span>${esc(course.name)}</span><span>${course.progress}%</span></div><div class="progress-line"><span style="width:${course.progress}%"></span></div></div><div class="panel-head" style="margin-top:30px"><h3>Upcoming exams</h3><a href="#/exams">View all</a></div>${db.exams.map(exam=>`<div class="list-item"><div class="item-main"><div class="item-icon">E</div><div><b>${esc(exam.title)}</b><small>${esc(exam.course)} · ${exam.date}</small></div></div><span class="pill">${exam.status}</span></div>`).join('')}</section><section class="panel"><div class="panel-head"><h3>Latest grades</h3><a href="#/grades">See grades</a></div><div class="list">${grades.length?grades.map(g=>`<div class="list-item"><div class="item-main"><div class="item-icon">✓</div><div><b>${esc(g.exam)}</b><small>${g.date}</small></div></div><strong>${g.score}%</strong></div>`).join(''):'<div class="empty">No grades yet.</div>'}</div><div class="panel-head" style="margin-top:28px"><h3>Notifications</h3></div>${notices.slice(0,2).map(n=>`<div class="notice"><div class="item-icon">i</div><p>${esc(n.text)}<small>${n.date}</small></p></div>`).join('')}</section></div>`); }
function studentCourses(){ const student=currentStudent(); const course=db.courses.find(c=>c.name===student?.course)||db.courses[0]; return dashboardShell('student',`<div class="page-title"><h1>My Courses</h1><p>Your payment is approved. Your active learning path is ready.</p></div><div class="courses-grid"><article class="course-card"><div class="course-top"><span class="pill green">Payment Approved</span><span class="pill">${course.level}</span></div><h3>${esc(course.name)}</h3><p>${esc(course.description)}</p><small style="display:block;color:var(--muted);margin-top:14px">Mr. Abdulrahman Mohammed</small><div class="progress-row"><div class="progress-meta"><span>Progress</span><span>${course.progress}%</span></div><div class="progress-line"><span style="width:${course.progress}%"></span></div></div><button class="btn btn-primary" data-course-details="${course.id}">View Course Details</button></article></div><div class="panel" style="margin-top:20px" data-course-detail hidden><div class="panel-head"><div><h3>${esc(course.name)} · Course Details</h3><p class="course-detail-description">${esc(course.description)}</p></div><button class="btn btn-plain btn-small" data-course-close>Close</button></div><div class="course-lessons" data-student-lessons="${course.id}"><div class="empty">Loading lessons...</div></div></div>`); }
function simpleStudentPage(path){ if(path==='exams') return dashboardShell('student', `<div class="page-title"><h1>Exams</h1><p>Prepare, complete, and review your assessments.</p></div><div class="panel"><div class="panel-head"><h3>Available & upcoming exams</h3></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Exam Title</th><th>Course</th><th>Date</th><th>Duration</th><th>Status</th><th></th></tr></thead><tbody>${db.exams.map(e=>`<tr><td><strong>${esc(e.title)}</strong></td><td>${esc(e.course)}</td><td>${e.date}</td><td>${esc(e.duration)}</td><td><span class="pill">${e.status}</span></td><td><button class="btn btn-primary btn-small" data-start-exam="${e.id}">Start Exam</button></td></tr>`).join('')}</tbody></table></div></div><div class="panel exam-workspace" data-exam-workspace hidden><div class="panel-head"><div><h3 data-active-exam-title>Exam</h3><small data-exam-timer>Time remaining: --:--</small></div><span class="pill" data-exam-progress>0 questions</span></div><form data-exam-form><div data-exam-questions></div><button class="btn btn-primary" type="submit">Submit Exam</button></form><div data-exam-result hidden></div></div>`);
 if(path==='grades') return dashboardShell('student', `<div class="page-title"><h1>Grades</h1><p>Track exam and assignment performance.</p></div><div class="stat-grid" style="grid-template-columns:repeat(3,1fr)"><div class="stat-card"><span class="stat-label">Course Average</span><strong>88%</strong><small>Strong performance</small></div><div class="stat-card"><span class="stat-label">Total Score</span><strong>88 / 100</strong><small>Across graded work</small></div><div class="stat-card"><span class="stat-label">Teacher Comment</span><strong style="font-size:17px">Keep going</strong><small>Mr. Abdulrahman</small></div></div><div class="panel"><div class="table-wrap"><table class="data-table"><thead><tr><th>Assessment</th><th>Course</th><th>Date</th><th>Score</th><th>Teacher Comment</th></tr></thead><tbody>${db.grades.filter(g=>g.student===session.id).map(g=>`<tr><td><strong>${g.exam}</strong></td><td>${g.course}</td><td>${g.date}</td><td><span class="pill green">${g.score}/${g.total}</span></td><td>${g.comment}</td></tr>`).join('')}</tbody></table></div></div>`);
 if(path==='attendance') return dashboardShell('student', `<div class="page-title"><h1>Attendance</h1><p>Consistency makes progress visible.</p></div><div class="stat-grid" style="grid-template-columns:repeat(3,1fr)"><div class="stat-card"><span class="stat-label">Attendance Percentage</span><strong>100%</strong><small>Excellent</small></div><div class="stat-card"><span class="stat-label">Present Days</span><strong>2</strong><small>Keep it up</small></div><div class="stat-card"><span class="stat-label">Absent / Late</span><strong>0 / 0</strong><small>No missed sessions</small></div></div><div class="panel"><div class="panel-head"><h3>Attendance history</h3></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Date</th><th>Course</th><th>Status</th><th>Teacher Note</th></tr></thead><tbody>${db.attendance.filter(a=>a.student===session.id).map(a=>`<tr><td>${a.date}</td><td>${a.course}</td><td><span class="pill green">${a.status}</span></td><td>${a.note||'—'}</td></tr>`).join('')}</tbody></table></div></div>`);
 if(path==='payments') return dashboardShell('student', `<div class="page-title"><h1>Choose Your Learning Package</h1><p>Pay first, then the teacher will review and unlock your full platform access.</p></div><div class="package-grid">${db.courses.map(c=>`<article class="package-card ${currentStudent().course===c.name?'selected':''}"><span class="pill">${esc(c.level)}</span><h3>${esc(c.name)}</h3><p>${esc(c.description)}</p><strong>${c.price} EGP</strong><button class="btn btn-soft btn-small" data-select-package="${esc(c.name)}">Select Package</button></article>`).join('')}</div><div class="two-col"><div class="panel"><div class="panel-head"><h3>InstaPay Payment</h3><span class="pill ${currentStudent().payment==='Approved'?'green':currentStudent().payment==='Rejected'?'red':''}">${currentStudent().payment}</span></div><div class="notice"><div class="item-icon">₱</div><p><strong>Send payment to</strong><small style="font-size:18px;color:var(--ink);font-weight:800;margin-top:7px">${INSTA_PAY}</small></p><button class="btn btn-soft btn-small" data-copy="${INSTA_PAY}">Copy</button></div><p style="color:var(--muted);font-size:13px;line-height:1.6">Choose a package above, transfer its price, then submit your transaction details. Course access remains locked until the teacher approves the payment.</p><form data-form="payment"><div class="field"><label>Selected Package</label><select name="course" data-package-select>${db.courses.map(c=>`<option value="${esc(c.name)}" ${currentStudent().course===c.name?'selected':''}>${esc(c.name)} · ${c.price} EGP</option>`).join('')}</select></div><div class="field"><label>Transaction Reference Number</label><input name="transaction" required /></div><div class="field"><label>Sender Phone Number</label><input name="sender" required /></div><div class="field"><label>Payment Date</label><input type="date" name="date" required /></div><button class="btn btn-primary" type="submit">Submit Payment Request</button></form></div><div class="panel"><div class="panel-head"><h3>Payment history</h3></div>${db.payments.filter(p=>p.student===session.id).map(p=>`<div class="list-item"><div><b>${p.course}</b><small>${p.date} · ${p.transaction}</small></div><span class="pill ${p.status==='Approved'?'green':p.status==='Rejected'?'red':''}">${p.status}</span></div>`).join('')||'<div class="empty">No payment requests yet.</div>'}</div></div>`);
 if(path==='my-code') return dashboardShell('student', `<div class="page-title"><h1>My Code</h1><p>Your code is the simplest way to access your account.</p></div><div class="panel" style="max-width:650px"><div class="icon-box">K</div><h3>${esc(currentStudent().name)}</h3><p style="color:var(--muted)">Your student code</p><div style="display:flex;align-items:center;gap:12px;margin:22px 0"><strong style="font:800 34px Manrope;letter-spacing:2px">${currentStudent().code}</strong><button class="btn btn-soft btn-small" data-copy="${currentStudent().code}">Copy Code</button></div><div class="notice"><div class="item-icon">i</div><p>Code status: <strong>Active</strong><small>Use this code on the Student Login page to enter your learning space.</small></p></div><p style="color:var(--muted);line-height:1.6">Linked course: <strong style="color:var(--ink)">${currentStudent().course}</strong></p></div>`);
 if(path==='profile') return dashboardShell('student', `<div class="page-title"><h1>Profile</h1><p>Keep your learning details up to date.</p></div><div class="panel" style="max-width:760px"><form data-form="profile"><div class="form-grid"><div class="field"><label>Full Name</label><input name="name" value="${esc(currentStudent().name)}" required /></div><div class="field"><label>Phone Number</label><input name="phone" value="${esc(currentStudent().phone)}" required /></div><div class="field"><label>Email Address</label><input name="email" value="${esc(currentStudent().email)}" type="email" required /></div><div class="field"><label>Grade</label><input name="grade" value="${esc(currentStudent().grade)}" required /></div><div class="field"><label>Parent Phone</label><input name="parentPhone" value="${esc(currentStudent().parentPhone)}" required /></div></div><button class="btn btn-primary" type="submit">Save Changes</button></form></div>`);
 return dashboardShell('student', `<div class="page-title"><h1>Settings</h1><p>Manage your account preferences.</p></div><div class="panel" style="max-width:760px"><div class="settings-row"><div><strong>Account Settings</strong><small>Keep your profile information current.</small></div><span class="pill green">Active</span></div><div class="settings-row"><div><strong>Password Settings</strong><small>Update your password securely.</small></div><button class="btn btn-soft btn-small" data-password>Change</button></div><div class="settings-row"><div><strong>Notification Settings</strong><small>Receive useful learning updates.</small></div><button class="switch on" data-switch><i></i></button></div><div class="settings-row"><div><strong>Language Settings</strong><small>Interface language</small></div><span class="pill">English</span></div><div class="settings-row"><strong>Logout</strong><button class="btn btn-danger btn-small" data-logout>Logout</button></div></div>`); }

function teacherDashboard(){ return dashboardShell('teacher', `<div class="dash-welcome"><div><h1 style="font-size:32px;letter-spacing:-1px">Good morning, Mr. Abdulrahman.</h1><p>Here is what needs your attention today.</p></div><a class="btn btn-primary" href="#/announcements">Create announcement →</a></div><div class="stat-grid"><div class="stat-card"><span class="stat-label">Total Students</span><strong>${db.students.length}</strong><small>All active profiles</small></div><div class="stat-card"><span class="stat-label">Active Courses</span><strong>${db.courses.filter(c=>c.status==='Active').length}</strong><small>Published courses</small></div><div class="stat-card"><span class="stat-label">Pending Payments</span><strong>${db.payments.filter(p=>p.status==='Pending').length}</strong><small>Need review</small></div><div class="stat-card"><span class="stat-label">Upcoming Exams</span><strong>${db.exams.length}</strong><small>This term</small></div><div class="stat-card"><span class="stat-label">Attendance Overview</span><strong>94%</strong><small>Across all courses</small></div></div><div class="dash-grid"><section class="panel"><div class="panel-head"><h3>Recent registrations</h3><a href="#/students">View students</a></div>${db.students.map(s=>`<div class="list-item"><div class="item-main"><div class="avatar">${initials(s.name)}</div><div><b>${esc(s.name)}</b><small>${esc(s.course)} · ${s.joined}</small></div></div><span class="pill green">${s.status}</span></div>`).join('')}</section><section class="panel"><div class="panel-head"><h3>Recent payment requests</h3><a href="#/teacher-payments">Review all</a></div>${db.payments.slice(-3).map(p=>{const s=db.students.find(x=>x.id===p.student);return `<div class="list-item"><div><b>${s?.name||'Student'}</b><small>${p.course} · ${p.amount} EGP</small></div><span class="pill ${p.status==='Approved'?'green':p.status==='Rejected'?'red':''}">${p.status}</span></div>`}).join('')}</section></div>`); }
function teacherStudents(){ return dashboardShell('teacher', `<div class="page-title" style="display:flex;justify-content:space-between;align-items:end"><div><h1>Students</h1><p>Manage profiles, codes, and learning records.</p></div><button class="btn btn-primary" data-add-student>Add Student</button></div><div class="panel"><div class="field" style="max-width:340px"><label>Search students</label><input data-student-search placeholder="Search by name, email, or code" /></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Student</th><th>Code</th><th>Course</th><th>Status</th><th>Payment</th><th>Actions</th></tr></thead><tbody id="students-body">${studentRows()}</tbody></table></div></div>`); }
function studentRows(filter=''){ return db.students.filter(s=>`${s.name} ${s.email} ${s.code}`.toLowerCase().includes(filter.toLowerCase())).map(s=>`<tr><td><strong>${esc(s.name)}</strong><small style="display:block;color:var(--muted)">${esc(s.email)}</small></td><td>${s.code}</td><td>${s.course}</td><td><span class="pill green">${s.status}</span></td><td><span class="pill ${s.payment==='Approved'?'green':s.payment==='Rejected'?'red':''}">${s.payment}</span></td><td><div style="display:flex;gap:8px;flex-wrap:wrap"><button class="btn btn-soft btn-small" data-edit-student="${s.id}">Edit</button><button class="btn btn-danger btn-small" data-delete-student="${s.id}">Delete</button></div></td></tr>`).join('') || '<tr><td colspan="6" class="empty">No matching students.</td></tr>'; }
function teacherCourses(){ return dashboardShell('teacher', `<div class="page-title" style="display:flex;justify-content:space-between;align-items:end"><div><h1>Courses</h1><p>Create structured learning paths and manage course lessons.</p></div><button class="btn btn-primary" data-add-course>Add Course</button></div><div class="courses-grid">${db.courses.map(c=>`<article class="course-card teacher-course-card"><div class="course-top"><span class="pill green">${c.status}</span><span class="pill">${c.students} students</span></div><h3>${esc(c.name)}</h3><p>${esc(c.description)}</p><small style="display:block;color:var(--muted);margin-top:14px">${c.level} · ${c.price} EGP</small><div class="progress-row"><div class="progress-meta"><span>Average progress</span><span>${c.progress}%</span></div><div class="progress-line"><span style="width:${c.progress}%"></span></div></div><div style="display:flex;gap:8px;flex-wrap:wrap"><button class="btn btn-primary btn-small" data-add-lesson="${c.id}">+ Add Lesson</button><button class="btn btn-soft btn-small" data-manage-lessons="${c.id}">Manage Lessons</button><button class="btn btn-soft btn-small" data-edit-course="${c.id}">Edit</button><button class="btn btn-danger btn-small" data-delete-course="${c.id}">Delete</button></div><div class="teacher-lessons" data-teacher-lessons="${c.id}" hidden><div class="lesson-manager-head"><strong>Course Lessons</strong><span class="pill">Ordered</span></div><div data-lesson-list="${c.id}"><div class="empty">Loading lessons...</div></div></div></article>`).join('')}</div>`); }
function teacherExams(){ return dashboardShell('teacher', `<div class="page-title" style="display:flex;justify-content:space-between;align-items:end"><div><h1>Exams</h1><p>Create assessments, add up to 100 questions, and review submissions.</p></div><button class="btn btn-primary" data-add-exam>Create Exam</button></div><div class="panel"><div class="table-wrap"><table class="data-table"><thead><tr><th>Exam Title</th><th>Course</th><th>Date</th><th>Duration</th><th>Questions</th><th>Status</th><th>Actions</th></tr></thead><tbody>${db.exams.map(e=>`<tr><td><strong>${esc(e.title)}</strong></td><td>${esc(e.course)}</td><td>${e.date}</td><td>${esc(e.duration)} min</td><td><span data-question-count="${e.id}">Loading</span></td><td><span class="pill">${e.status}</span></td><td><button class="btn btn-primary btn-small" data-manage-questions="${e.id}">Manage Questions</button> <button class="btn btn-soft btn-small" data-edit-exam="${e.id}">Edit Time</button> <button class="btn btn-soft btn-small" data-toggle-exam="${e.id}">${e.status==='Available'?'Hide':'Publish'}</button></td></tr>`).join('')}</tbody></table></div></div><div class="panel question-panel" data-question-panel hidden><div class="panel-head"><div><h3 data-question-title>Questions</h3><p>Add questions, answer choices, and the correct answer.</p></div><button class="btn btn-primary btn-small" data-add-question>Add Question</button></div><div data-question-list><div class="empty">Select an exam to manage its questions.</div></div></div>`); }
function teacherGrades(){ return dashboardShell('teacher', `<div class="page-title" style="display:flex;justify-content:space-between;align-items:end"><div><h1>Grades</h1><p>Publish scores and helpful teacher comments.</p></div><button class="btn btn-primary" data-add-grade>Add Grade</button></div><div class="panel"><div class="table-wrap"><table class="data-table"><thead><tr><th>Student</th><th>Assessment</th><th>Course</th><th>Score</th><th>Comment</th></tr></thead><tbody>${db.grades.map(g=>`<tr><td>${db.students.find(s=>s.id===g.student)?.name||'Student'}</td><td>${g.exam}</td><td>${g.course}</td><td><span class="pill green">${g.score}/${g.total}</span></td><td>${g.comment}</td></tr>`).join('')}</tbody></table></div></div>`); }
function teacherAttendance(){ return dashboardShell('teacher', `<div class="page-title" style="display:flex;justify-content:space-between;align-items:end"><div><h1>Attendance</h1><p>Keep every learner's consistency visible.</p></div><button class="btn btn-primary" data-add-attendance>Record Attendance</button></div><div class="panel"><div class="table-wrap"><table class="data-table"><thead><tr><th>Date</th><th>Student</th><th>Course</th><th>Status</th><th>Note</th></tr></thead><tbody>${db.attendance.map(a=>`<tr><td>${a.date}</td><td>${db.students.find(s=>s.id===a.student)?.name||'Student'}</td><td>${a.course}</td><td><span class="pill green">${a.status}</span></td><td>${a.note||'—'}</td></tr>`).join('')}</tbody></table></div></div>`); }
function teacherPayments(){ return dashboardShell('teacher', `<div class="page-title"><h1>Payments</h1><p>Review InstaPay requests and keep payment records accurate.</p></div><div class="panel"><div class="table-wrap"><table class="data-table"><thead><tr><th>Student</th><th>Course</th><th>Amount</th><th>Transaction</th><th>Sender</th><th>Status</th><th>Actions</th></tr></thead><tbody>${db.payments.map(p=>`<tr><td>${db.students.find(s=>s.id===p.student)?.name||'Student'}</td><td>${p.course}</td><td>${p.amount} EGP</td><td>${p.transaction}</td><td>${p.sender}</td><td><span class="pill ${p.status==='Approved'?'green':p.status==='Rejected'?'red':''}">${p.status}</span></td><td>${p.status==='Pending'?`<button class="btn btn-soft btn-small" data-payment-action="approve" data-payment-id="${p.id}">Approve</button> <button class="btn btn-danger btn-small" data-payment-action="reject" data-payment-id="${p.id}">Reject</button>`:'—'}</td></tr>`).join('')}</tbody></table></div></div>`); }
function announcements(){ return dashboardShell('teacher', `<div class="page-title" style="display:flex;justify-content:space-between;align-items:end"><div><h1>Announcements</h1><p>Share timely updates with your learning community.</p></div><button class="btn btn-primary" data-add-announcement>New Announcement</button></div><div class="two-col">${db.announcements.map(a=>`<div class="panel"><div class="panel-head"><span class="pill">${a.audience}</span><small>${a.date}</small></div><h3>${a.title}</h3><p style="color:var(--muted);line-height:1.6">${a.body}</p><button class="btn btn-danger btn-small" data-delete-announcement="${a.id}">Delete</button></div>`).join('')}</div>`); }
function teacherSettings(){ return dashboardShell('teacher', `<div class="page-title"><h1>Settings</h1><p>Manage the teacher workspace preferences.</p></div><div class="panel" style="max-width:760px"><div class="settings-row"><div><strong>Account Settings</strong><small>Mr. Abdulrahman Mohammed</small></div><span class="pill green">Teacher access</span></div><div class="settings-row"><div><strong>Notification Settings</strong><small>Receive payment and registration updates.</small></div><button class="switch on" data-switch><i></i></button></div><div class="settings-row"><div><strong>Language Settings</strong><small>Interface language</small></div><span class="pill">English</span></div><div class="settings-row"><strong>Logout</strong><button class="btn btn-danger btn-small" data-logout>Logout</button></div></div>`); }

function render(){ const path=route(); const studentRoutes=['student-dashboard','courses','exams','grades','attendance','my-code','profile','settings']; if(session?.role==='student' && currentStudent()?.payment!=='Approved' && studentRoutes.includes(path)){ navigate('payments'); return; } if((path==='home'||path==='login'||path==='register'||path==='teacher-login') && session) { if(path==='home'){} } if(path==='home') app.innerHTML=homePage(); else if(path==='login') app.innerHTML=session?studentDashboard():authPage(); else if(path==='register') app.innerHTML=session?studentDashboard():registerPage(); else if(path==='teacher-login') app.innerHTML=session?.role==='teacher'?teacherDashboard():authPage('teacher'); else if(session?.role==='student' && [...studentRoutes,'payments'].includes(path)) app.innerHTML=path==='student-dashboard'?studentDashboard():path==='courses'?studentCourses():simpleStudentPage(path); else if(session?.role==='teacher' && ['teacher-dashboard','students','teacher-courses','teacher-exams','teacher-grades','teacher-attendance','teacher-payments','announcements','settings'].includes(path)) app.innerHTML=path==='teacher-dashboard'?teacherDashboard():path==='students'?teacherStudents():path==='teacher-courses'?teacherCourses():path==='teacher-exams'?teacherExams():path==='teacher-grades'?teacherGrades():path==='teacher-attendance'?teacherAttendance():path==='teacher-payments'?teacherPayments():path==='announcements'?announcements():teacherSettings(); else { session=null; localStorage.removeItem(SESSION_KEY); navigate(path==='student-dashboard'?'login':path.startsWith('teacher')?'teacher-login':'home'); return; } bindEvents(); if(path==='teacher-exams' && session?.role==='teacher' && !teacherExamsHydrated){ teacherExamsHydrated=true; apiRequest('/api/teacher/exams').then(result=>{db.exams=result.exams.map(exam=>({...exam,course:exam.course}));save();render();}).catch(error=>{teacherExamsHydrated=false;toast(error.message,'error');}); } if(path==='teacher-payments' && session?.role==='teacher' && !teacherPaymentsHydrated){ teacherPaymentsHydrated=true; apiRequest('/api/teacher/payments').then(result=>{db.payments=result.payments.map(payment=>({...payment,student:payment.student_id}));save();render();}).catch(error=>{teacherPaymentsHydrated=false;toast(error.message,'error');}); } }

function formData(form){ return Object.fromEntries(new FormData(form).entries()); }
function error(form,message){ const node=form.querySelector('[data-form-error]'); if(node) node.innerHTML=`<div class="error">${esc(message)}</div>`; }
function bindEvents(){ document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>navigate(b.dataset.go)); document.querySelectorAll('[data-logout]').forEach(b=>b.onclick=()=>{session=null;localStorage.removeItem(SESSION_KEY);navigate('home');toast('You have been logged out.');}); document.querySelector('[data-menu]')?.addEventListener('click',()=>document.querySelector('.nav').classList.toggle('open')); document.querySelector('[data-sidebar-toggle]')?.addEventListener('click',()=>document.querySelector('[data-sidebar]').classList.toggle('open')); document.querySelectorAll('[data-copy]').forEach(b=>b.onclick=()=>navigator.clipboard?.writeText(b.dataset.copy).then(()=>toast('Copied to clipboard.'))); document.querySelectorAll('[data-auth-tab]').forEach(tab=>tab.onclick=()=>{document.querySelectorAll('[data-auth-tab]').forEach(t=>t.classList.remove('active'));tab.classList.add('active');document.querySelectorAll('[data-auth-panel]').forEach(p=>p.style.display=p.dataset.authPanel===tab.dataset.authTab?'block':'none');}); document.querySelector('[data-form="teacher-login"]')?.addEventListener('submit',e=>{e.preventDefault();const data=formData(e.target);if(data.code!==TEACHER_CODE){error(e.target,'That teacher code is not valid.');return}session={role:'teacher'};localStorage.setItem(SESSION_KEY,JSON.stringify(session));navigate('teacher-dashboard');}); document.querySelector('[data-form="student-code"]')?.addEventListener('submit',e=>{e.preventDefault();const student=db.students.find(s=>s.code.toLowerCase()===formData(e.target).code.trim().toLowerCase()&&s.status==='Active');if(!student){error(e.target,'We could not find an active student with that code.');return}session={role:'student',id:student.id};localStorage.setItem(SESSION_KEY,JSON.stringify(session));navigate('student-dashboard');}); document.querySelector('[data-form="student-data"]')?.addEventListener('submit',e=>{e.preventDefault();const data=formData(e.target);const student=db.students.find(s=>s.email.toLowerCase()===data.email.toLowerCase()&&s.password===data.password);if(!student){error(e.target,'Email or password is incorrect.');return}session={role:'student',id:student.id};localStorage.setItem(SESSION_KEY,JSON.stringify(session));navigate('student-dashboard');}); document.querySelector('[data-form="register"]')?.addEventListener('submit',e=>{e.preventDefault();const data=formData(e.target);if(data.password!==data.confirm){error(e.target,'Passwords do not match.');return}if(db.students.some(s=>s.email.toLowerCase()===data.email.toLowerCase())){error(e.target,'An account with this email already exists.');return}const student={id:`s${Date.now()}`,name:data.name,phone:data.phone,email:data.email,grade:data.grade,course:data.course,parentPhone:data.parentPhone,password:data.password,code:`EZ-${Math.floor(1000+Math.random()*8999)}`,status:'Active',payment:'Pending',joined:data.joined};db.students.push(student);save();session={role:'student',id:student.id};localStorage.setItem(SESSION_KEY,JSON.stringify(session));toast('Account created successfully.');navigate('payments');}); document.querySelector('[data-form="payment"]')?.addEventListener('submit',e=>{e.preventDefault();const data=formData(e.target);db.payments.push({id:`p${Date.now()}`,student:session.id,course:data.course,amount:db.courses.find(c=>c.name===data.course)?.price||0,transaction:data.transaction,sender:data.sender,date:data.date,status:'Pending',reason:''});currentStudent().payment='Pending';save();toast('Your payment request has been submitted and will be reviewed by the teacher.');render();}); document.querySelector('[data-form="profile"]')?.addEventListener('submit',e=>{e.preventDefault();Object.assign(currentStudent(),formData(e.target));save();toast('Profile changes saved.');render();}); document.querySelector('[data-course-details]')?.addEventListener('click',()=>{document.querySelector('[data-course-detail]').hidden=false;}); document.querySelector('[data-course-close]')?.addEventListener('click',()=>{document.querySelector('[data-course-detail]').hidden=true;}); document.querySelector('[data-switch]')?.addEventListener('click',e=>e.currentTarget.classList.toggle('on')); document.querySelector('[data-student-search]')?.addEventListener('input',e=>{document.querySelector('#students-body').innerHTML=studentRows(e.target.value);bindEvents();}); document.querySelectorAll('[data-edit-student]').forEach(b=>b.onclick=()=>editStudent(b.dataset.editStudent)); document.querySelector('[data-add-student]')?.addEventListener('click',()=>editStudent()); document.querySelector('[data-add-course]')?.addEventListener('click',()=>courseModal()); document.querySelectorAll('[data-edit-course]').forEach(b=>b.onclick=()=>courseModal(b.dataset.editCourse)); document.querySelectorAll('[data-delete-course]').forEach(b=>b.onclick=()=>confirmAction('Delete this course?','This will remove the course from the teacher view.',()=>{db.courses=db.courses.filter(c=>c.id!==b.dataset.deleteCourse);save();toast('Course deleted.');render();})); document.querySelector('[data-add-exam]')?.addEventListener('click',()=>examModal()); document.querySelectorAll('[data-toggle-exam]').forEach(b=>b.onclick=()=>{const exam=db.exams.find(e=>e.id===b.dataset.toggleExam);exam.status=exam.status==='Available'?'Hidden':'Available';save();render();toast(`Exam ${exam.status==='Available'?'published':'hidden'}.`);}); document.querySelector('[data-add-grade]')?.addEventListener('click',()=>gradeModal()); document.querySelector('[data-add-attendance]')?.addEventListener('click',()=>attendanceModal()); document.querySelectorAll('[data-payment-action]').forEach(b=>b.onclick=()=>paymentAction(b.dataset.paymentId,b.dataset.paymentAction)); document.querySelector('[data-add-announcement]')?.addEventListener('click',()=>announcementModal()); document.querySelectorAll('[data-delete-announcement]').forEach(b=>b.onclick=()=>confirmAction('Delete announcement?','This announcement will be removed for all students.',()=>{db.announcements=db.announcements.filter(a=>a.id!==b.dataset.deleteAnnouncement);save();render();})); document.querySelectorAll('[data-start-exam]').forEach(b=>b.onclick=()=>toast('Exam workspace is ready. Submit your answers before the timer ends.')); }

function showFormModal(title, fields, submitText, callback){ document.querySelector('#modal-region').innerHTML=`<div class="modal-backdrop"><div class="modal animate-in"><h3>${title}</h3><form data-modal-form>${fields}<div class="modal-actions"><button type="button" class="btn btn-plain" data-close-modal>Cancel</button><button class="btn btn-primary" type="submit">${submitText}</button></div></form></div></div>`;document.querySelector('[data-close-modal]').onclick=()=>document.querySelector('#modal-region').innerHTML='';document.querySelector('[data-modal-form]').onsubmit=e=>{e.preventDefault();callback(formData(e.target));document.querySelector('#modal-region').innerHTML='';}; }
const modalField=(label,name,value='',type='text')=>`<div class="field"><label>${label}</label><input name="${name}" type="${type}" value="${esc(value)}" required /></div>`;
function editStudent(id){ const student=db.students.find(s=>s.id===id)||{};showFormModal(id?'Edit student':'Add student',`<div class="form-grid">${modalField('Full Name','name',student.name)}${modalField('Email Address','email',student.email,'email')}${modalField('Phone Number','phone',student.phone)}${modalField('Parent or Guardian Phone','parentPhone',student.parentPhone)}${id?modalField('Grade','grade',student.grade):''}${id?modalField('Course','course',student.course||db.courses[0]?.name):''}${id?modalField('Student Code','code',student.code):''}${id?modalField('Password (optional)','password','') : ''}</div>`,'Save changes',data=>{}); }
function courseModal(id){const c=db.courses.find(x=>x.id===id)||{};showFormModal(id?'Edit course':'Add course',`${modalField('Course Name','name',c.name)}${modalField('Grade or Level','level',c.level)}${modalField('Course Price','price',c.price,'number')}<div class="field"><label>Course Description</label><textarea name="description" required>${esc(c.description||'')}</textarea></div>`,'Save course',data=>{if(id)Object.assign(c,{...data,price:Number(data.price)});else db.courses.push({...data,id:`c${Date.now()}`,price:Number(data.price),status:'Active',progress:0,students:0});save();toast('Course saved.');render();});}
function examModal(exam = null){showFormModal(exam?'Edit exam':'Create exam',`${exam?`<input type="hidden" name="examId" value="${esc(exam.id)}" />`:''}${modalField('Exam Title','title',exam?.title)}${modalField('Course Name','course',exam?.course || db.courses[0]?.name)}${modalField('Exam Date','date',exam?.date || '', 'date')}${modalField('Duration in minutes','duration',parseInt(exam?.duration,10)||45,'number')}`,exam?'Save exam':'Create exam',data=>{});}
function questionModal(examId, question = null) { showFormModal(question?'Edit question':'Add question',`<input type="hidden" name="examId" value="${esc(examId)}" />${question?.id?`<input type="hidden" name="questionId" value="${esc(question.id)}" />`:''}${modalField('Question','prompt',question?.prompt)}${modalField('Answer A','optionA',question?.options?.[0])}${modalField('Answer B','optionB',question?.options?.[1])}${modalField('Answer C','optionC',question?.options?.[2])}${modalField('Answer D','optionD',question?.options?.[3])}<div class="field"><label>Correct Answer</label><select name="correctAnswer"><option value="0" ${question?.correctAnswer==='0'?'selected':''}>Answer A</option><option value="1" ${question?.correctAnswer==='1'?'selected':''}>Answer B</option><option value="2" ${question?.correctAnswer==='2'?'selected':''}>Answer C</option><option value="3" ${question?.correctAnswer==='3'?'selected':''}>Answer D</option></select></div>${modalField('Points','points',question?.points || 1,'number')}` ,question?'Save Question':'Add Question',data=>{}); }
async function loadQuestionList(examId) { const target=document.querySelector('[data-question-list]'); if(!target)return; try { const result=await apiRequest(`/api/teacher/exams/${examId}/questions`); document.querySelector('[data-question-title]').textContent=`Questions · ${result.questions.length}/100`; document.querySelector(`[data-question-count="${examId}"]`).textContent=result.questions.length; target.innerHTML=result.questions.length?result.questions.map((q,index)=>`<div class="question-row"><div><span class="pill">Question ${index+1}</span><strong>${esc(q.prompt)}</strong><small>${q.options.map((option,i)=>`${String.fromCharCode(65+i)}. ${esc(option)}`).join(' · ')}</small></div><div><button class="btn btn-soft btn-small" data-edit-question="${q.id}" data-exam-id="${examId}">Edit</button><button class="btn btn-danger btn-small" data-delete-question="${q.id}" data-exam-id="${examId}">Delete</button></div></div>`).join(''):'<div class="empty">No questions yet. Add the first question.</div>'; } catch(error) { target.innerHTML=`<div class="error">${esc(error.message)}</div>`; } }
async function startExam(examId) { const workspace=document.querySelector('[data-exam-workspace]'); if(!workspace)return; try { const result=await apiRequest(`/api/student/exams/${examId}`); workspace.hidden=false; workspace.dataset.examId=examId; document.querySelector('[data-active-exam-title]').textContent=result.exam.title; document.querySelector('[data-exam-progress]').textContent=`${result.questions.length} questions`; const questions=document.querySelector('[data-exam-questions]'); questions.innerHTML=result.questions.length?result.questions.map((q,index)=>`<fieldset class="exam-question"><legend>${index+1}. ${esc(q.prompt)}</legend>${q.options.map((option,i)=>`<label><input type="radio" name="question-${q.id}" value="${i}" required /> <span>${String.fromCharCode(65+i)}. ${esc(option)}</span></label>`).join('')}</fieldset>`).join(''):'<div class="empty">This exam has no questions yet.</div>'; if(result.submission){document.querySelector('[data-exam-result]').hidden=false;document.querySelector('[data-exam-result]').innerHTML=`<div class="notice"><div class="item-icon">✓</div><p>You already submitted this exam.<small>Score: ${result.submission.score}</small></p></div>`;} const minutes=parseInt(result.exam.duration,10)||45; let remaining=minutes*60; clearInterval(window.examTimer); const timer=document.querySelector('[data-exam-timer]'); window.examTimer=setInterval(()=>{remaining-=1;timer.textContent=`Time remaining: ${Math.floor(remaining/60)}:${String(remaining%60).padStart(2,'0')}`;if(remaining<=0){clearInterval(window.examTimer);document.querySelector('[data-exam-form]')?.requestSubmit();}},1000); } catch(error) { toast(error.message,'error'); } }
function gradeModal(){showFormModal('Add grade',`${modalField('Student Code','code',db.students[0]?.code)}${modalField('Assessment','exam')}${modalField('Course','course',db.courses[0]?.name)}${modalField('Score','score','','number')}<div class="field"><label>Teacher Comment</label><textarea name="comment" required></textarea></div>`,'Publish grade',data=>{const student=db.students.find(s=>s.code===data.code);if(!student){toast('Student code was not found.','error');return}db.grades.push({student:student.id,exam:data.exam,course:data.course,score:Number(data.score),total:100,date:new Date().toISOString().slice(0,10),comment:data.comment});save();toast('Grade published.');render();});}
function attendanceModal(){showFormModal('Record attendance',`${modalField('Student Code','code',db.students[0]?.code)}${modalField('Course','course',db.courses[0]?.name)}${modalField('Date','date',new Date().toISOString().slice(0,10),'date')}<div class="field"><label>Status</label><select name="status"><option>Present</option><option>Absent</option><option>Late</option></select></div>${modalField('Teacher Note','note')}`,'Save record',data=>{const student=db.students.find(s=>s.code===data.code);if(!student){toast('Student code was not found.','error');return}db.attendance.push({...data,student:student.id});save();toast('Attendance record saved.');render();});}
function paymentAction(id,action){const payment=db.payments.find(p=>p.id===id);if(action==='reject'){showFormModal('Reject payment','<div class="field"><label>Reason for rejection</label><textarea name="reason" required></textarea></div>','Reject payment',async data=>{try { await apiRequest(`/api/teacher/payments/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'Rejected',reason:data.reason})}); const found=db.payments.find(p=>p.id===id); if(found){ found.status='Rejected'; found.reason=data.reason; } const s=db.students.find(x=>x.id===payment.student); if(s){ s.payment='Rejected'; } save(); toast('Payment rejected.'); teacherPaymentsHydrated=false; render(); } catch(error) { toast(error.message,'error'); } });return}confirmAction('Approve payment?','The student will see this payment as approved.',async()=>{try { await apiRequest(`/api/teacher/payments/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'Approved'})}); const found=db.payments.find(p=>p.id===id); if(found){ found.status='Approved'; } const s=db.students.find(x=>x.id===payment.student); if(s){ s.payment='Approved'; } save(); toast('Payment approved.'); teacherPaymentsHydrated=false; render(); } catch(error) { toast(error.message,'error'); } });}
function announcementModal(){showFormModal('New announcement',`${modalField('Title','title')}${modalField('Audience','audience','All students')}<div class="field"><label>Message</label><textarea name="body" required></textarea></div>${modalField('Date','date',new Date().toISOString().slice(0,10),'date')}`,'Publish announcement',data=>{db.announcements.push({...data,id:`a${Date.now()}`});save();toast('Announcement published.');render();});}

function lessonModal(courseId, lesson = null) { const course=db.courses.find(item=>item.id===courseId); const lessonTitle=lesson?'Edit Lesson':'Add Lesson'; showFormModal(`${lessonTitle} · ${course?.name || 'Course'}`, `<input type="hidden" name="courseId" value="${esc(courseId)}" />${lesson?.id?`<input type="hidden" name="lessonId" value="${esc(lesson.id)}" />`:''}<div class="form-grid">${modalField('Lesson Title','title',lesson?.title)}<div class="field"><label>Lesson Type</label><select name="type"><option ${lesson?.type==='Video'?'selected':''}>Video</option><option ${lesson?.type==='PDF'?'selected':''}>PDF</option><option ${!lesson||lesson?.type==='External Link'?'selected':''}>External Link</option></select></div><div class="field full"><label>Lesson Description</label><textarea name="description" required>${esc(lesson?.description || '')}</textarea></div><div class="field full"><label>Lesson Link</label><input name="link" type="url" placeholder="https://youtube.com/... or https://drive.google.com/..." value="${esc(lesson?.link || '')}" required /></div>${modalField('Lesson Order','order',lesson?.order ?? 1,'number')}</div>`, lesson?'Save Lesson':'Add Lesson', data => {}); }
async function loadTeacherLessons(courseId) { const target=document.querySelector(`[data-lesson-list="${courseId}"]`); if(!target)return; try { const result=await apiRequest(`/api/courses/${courseId}/lessons`); target.innerHTML=result.lessons.length?result.lessons.map(lesson=>`<div class="lesson-row"><div class="lesson-meta"><span class="lesson-type">${esc(lesson.type)}</span><strong>${esc(lesson.order)}. ${esc(lesson.title)}</strong><small>${esc(lesson.description)}</small></div><div class="lesson-actions"><a class="btn btn-soft btn-small" href="${esc(lesson.link)}" target="_blank" rel="noopener">Open</a><button class="btn btn-soft btn-small" data-edit-lesson="${esc(lesson.id)}" data-course-id="${esc(courseId)}">Edit</button><button class="btn btn-danger btn-small" data-delete-lesson="${esc(lesson.id)}" data-course-id="${esc(courseId)}">Delete</button></div></div>`).join(''):'<div class="empty">No lessons added yet.</div>'; } catch(error) { target.innerHTML=`<div class="error">${esc(error.message)}</div>`; } }
async function loadStudentLessons(courseId) { const target=document.querySelector(`[data-student-lessons="${courseId}"]`); if(!target)return; try { const result=await apiRequest(`/api/courses/${courseId}/lessons`); target.innerHTML=result.lessons.length?`<h4>Lessons</h4><div class="list">${result.lessons.map(lesson=>`<div class="lesson-row student-lesson"><div class="lesson-meta"><span class="lesson-type">${esc(lesson.type)}</span><strong>${esc(lesson.order)}. ${esc(lesson.title)}</strong><small>${esc(lesson.description)}</small></div><a class="btn btn-primary btn-small" href="${esc(lesson.link)}" target="_blank" rel="noopener">${lesson.type==='Video'?'Watch Lesson':'Open Lesson'} →</a></div>`).join('')}</div>`:'<div class="empty">No lessons are available for this course yet.</div>'; } catch(error) { target.innerHTML=`<div class="error">${esc(error.message)}</div>`; } }

const apiRequest = async (url, options = {}) => { const response = await fetch(url, { credentials:'include', ...options }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || 'Request failed.'); return data; };
const apiJson = (url, body) => apiRequest(url, { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(body) });
const syncStudent = student => { if (!student) return; const existing = db.students.find(item => item.id === student.id); const mapped = {...student, parentPhone:student.parentPhone, code:student.code, status:student.status, payment:student.payment}; if (existing) Object.assign(existing, mapped); else db.students.push(mapped); save(); };
const showApiError = (form, message) => { const target = form.querySelector('[data-form-error]'); if (target) target.innerHTML = `<div class="error">${esc(message)}</div>`; else toast(message, 'error'); };
const renderExamResult = result => { const resultBox=document.querySelector('[data-exam-result]'); if(!resultBox)return; const corrections=result.incorrect.map(item=>{const selected=item.selected===null?'Not answered':`Answer ${String.fromCharCode(65+Number(item.selected))}`;return `<div class="correction-item"><b>${esc(item.prompt)}</b><small>Your answer: ${selected}</small><small class="correct-answer">Correct answer: ${esc(item.correctText)}</small></div>`;}).join(''); resultBox.hidden=false; resultBox.innerHTML='<div class="exam-result"><h3>Exam Result</h3><strong>'+result.score+' / '+result.total+'</strong><p>'+(result.score===result.total?'Perfect score. Excellent work.':'Your answers have been checked. Review the corrections below.')+'</p>'+(result.incorrect.length?'<div class="correction-list"><h4>Corrections</h4>'+corrections+'</div>':'<div class="notice"><div class="item-icon">✓</div><p>All answers are correct.</p></div>')+'</div>'; };

document.addEventListener('submit', async event => {
  const form = event.target;
  const kind = form.dataset.form;
  if (form.matches('[data-exam-form]')) { event.preventDefault(); event.stopImmediatePropagation(); try { const answers={}; new FormData(form).forEach((value,key)=>{answers[key.replace('question-','')]=value;}); const result=await apiRequest(`/api/student/exams/${form.closest('[data-exam-workspace]').dataset.examId}/submit`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answers})}); clearInterval(window.examTimer); renderExamResult(result); toast('Exam submitted and graded.'); return; } catch(error) { showApiError(form,error.message); return; } }
  if (!kind && !form.matches('[data-modal-form]')) return;
  event.preventDefault();
  event.stopImmediatePropagation();
  try {
    const data = formData(form);
    if (kind === 'student-code') { const result = await apiJson('/api/auth/student-code', data); syncStudent(result.student); session={role:'student',id:result.student.id}; localStorage.setItem(SESSION_KEY,JSON.stringify(session)); navigate(result.student.payment==='Approved'?'student-dashboard':'payments'); return; }
    if (kind === 'student-data') { const result = await apiJson('/api/auth/student-data', data); syncStudent(result.student); session={role:'student',id:result.student.id}; localStorage.setItem(SESSION_KEY,JSON.stringify(session)); navigate(result.student.payment==='Approved'?'student-dashboard':'payments'); return; }
    if (kind === 'teacher-login') { const result = await apiJson('/api/auth/teacher', data); session={role:'teacher'}; localStorage.setItem(SESSION_KEY,JSON.stringify(session)); navigate('teacher-dashboard'); return; }
    if (kind === 'register') { const result = await apiRequest('/api/students',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}); syncStudent(result.student); session={role:'student',id:result.student.id}; localStorage.setItem(SESSION_KEY,JSON.stringify(session)); toast('Account created successfully.'); navigate('payments'); return; }
    if (kind === 'payment') { const payload = new FormData(); Object.entries(data).forEach(([key,value])=>payload.append(key,value)); const result=await apiRequest('/api/student/payments',{method:'POST',body:payload}); syncStudent({...currentStudent(),payment:result.status,course:result.course}); toast(result.message); render(); return; }
    if (!form.matches('[data-modal-form]')) return;
    const title = (document.querySelector('#modal-region .modal h3')?.textContent || '').toLowerCase();
    if (title.includes('lesson')) { const endpoint=data.lessonId?`/api/teacher/lessons/${data.lessonId}`:'/api/teacher/lessons'; await apiRequest(endpoint,{method:data.lessonId?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}); toast(data.lessonId?'Lesson updated.':'Lesson added.'); document.querySelector('#modal-region').innerHTML=''; render(); setTimeout(()=>loadTeacherLessons(data.courseId),0); return; }
    if (title.includes('question')) { const options=[data.optionA,data.optionB,data.optionC,data.optionD]; if(options.some(option=>!option?.trim()))throw new Error('All four answers are required.'); const payload={prompt:data.prompt,options,correctAnswer:data.correctAnswer,points:data.points}; const endpoint=data.questionId?`/api/teacher/exam-questions/${data.questionId}`:`/api/teacher/exams/${data.examId}/questions`; await apiRequest(endpoint,{method:data.questionId?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}); document.querySelector('#modal-region').innerHTML=''; toast(data.questionId?'Question updated.':'Question added.'); loadQuestionList(data.examId); return; }
    if (title.includes('exam')) { const endpoint=data.examId?`/api/teacher/exams/${data.examId}`:'/api/teacher/exams'; await apiRequest(endpoint,{method:data.examId?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...data,courseId:data.course})}); document.querySelector('#modal-region').innerHTML=''; teacherExamsHydrated=false; toast(data.examId?'Exam updated.':'Exam published.'); render(); return; }
    if (title.includes('student')) { const result=await apiJson('/api/teacher/students',data); syncStudent(result.student); document.querySelector('#modal-region').innerHTML=''; toast(`Student created. Generated code: ${result.student.code}${result.generatedPassword ? ` · Password: ${result.generatedPassword}` : ''}`); render(); return; }
    if (title.includes('course')) { const result=await apiJson('/api/courses',data); db.courses.push(result.course); save(); toast('Course saved.'); render(); return; }
    if (title.includes('grade')) { await apiJson('/api/teacher/grades',{...data,courseId:data.course}); toast('Grade published.'); document.querySelector('#modal-region').innerHTML=''; render(); return; }
    if (title.includes('attendance')) { await apiJson('/api/teacher/attendance',{...data,courseId:data.course}); toast('Attendance record saved.'); document.querySelector('#modal-region').innerHTML=''; render(); return; }
    if (title.includes('announcement')) { await apiJson('/api/teacher/announcements',data); toast('Announcement published.'); document.querySelector('#modal-region').innerHTML=''; render(); return; }
    if (title.includes('reject payment')) { toast('Payment rejected.'); document.querySelector('#modal-region').innerHTML=''; render(); }
  } catch (error) { showApiError(form,error.message); }
}, true);

document.addEventListener('click', async event => {
  const packageButton = event.target.closest('[data-select-package]');
  if (packageButton) { event.preventDefault(); event.stopImmediatePropagation(); const select=document.querySelector('[data-package-select]'); if(select){select.value=packageButton.dataset.selectPackage;document.querySelectorAll('.package-card').forEach(card=>card.classList.remove('selected'));packageButton.closest('.package-card')?.classList.add('selected');} return; }
  const manageQuestions = event.target.closest('[data-manage-questions]');
  if (manageQuestions) { event.preventDefault(); event.stopImmediatePropagation(); const panel=document.querySelector('[data-question-panel]'); panel.hidden=false; panel.dataset.examId=manageQuestions.dataset.manageQuestions; document.querySelector('[data-add-question]').dataset.examId=manageQuestions.dataset.manageQuestions; await loadQuestionList(manageQuestions.dataset.manageQuestions); panel.scrollIntoView({behavior:'smooth',block:'start'}); return; }
  const editExam = event.target.closest('[data-edit-exam]');
  if (editExam) { event.preventDefault(); event.stopImmediatePropagation(); const exam=db.exams.find(item=>item.id===editExam.dataset.editExam); if(exam)examModal(exam); return; }
  const addQuestion = event.target.closest('[data-add-question]');
  if (addQuestion) { event.preventDefault(); event.stopImmediatePropagation(); questionModal(addQuestion.dataset.examId); return; }
  const editQuestion = event.target.closest('[data-edit-question]');
  if (editQuestion) { event.preventDefault(); event.stopImmediatePropagation(); try { const result=await apiRequest(`/api/teacher/exams/${editQuestion.dataset.examId}/questions`); const question=result.questions.find(item=>item.id===editQuestion.dataset.editQuestion); if(question)questionModal(editQuestion.dataset.examId,question); } catch(error) { toast(error.message,'error'); } return; }
  const deleteQuestion = event.target.closest('[data-delete-question]');
  if (deleteQuestion) { event.preventDefault(); event.stopImmediatePropagation(); confirmAction('Delete question?','This question and its answer key will be removed.',async()=>{try{await apiRequest(`/api/teacher/exam-questions/${deleteQuestion.dataset.deleteQuestion}`,{method:'DELETE'});toast('Question deleted.');loadQuestionList(deleteQuestion.dataset.examId);}catch(error){toast(error.message,'error');}}); return; }
  const startExamButton = event.target.closest('[data-start-exam]');
  if (startExamButton) { event.preventDefault(); event.stopImmediatePropagation(); startExam(startExamButton.dataset.startExam); return; }
  const addLesson = event.target.closest('[data-add-lesson]');
  if (addLesson) { event.preventDefault(); event.stopImmediatePropagation(); lessonModal(addLesson.dataset.addLesson); return; }
  const manageLessons = event.target.closest('[data-manage-lessons]');
  if (manageLessons) { event.preventDefault(); event.stopImmediatePropagation(); const panel=document.querySelector(`[data-teacher-lessons="${manageLessons.dataset.manageLessons}"]`); if(panel){ panel.hidden=!panel.hidden; if(!panel.hidden)loadTeacherLessons(manageLessons.dataset.manageLessons); } return; }
  const editLesson = event.target.closest('[data-edit-lesson]');
  if (editLesson) { event.preventDefault(); event.stopImmediatePropagation(); try { const result=await apiRequest(`/api/courses/${editLesson.dataset.courseId}/lessons`); const lesson=result.lessons.find(item=>item.id===editLesson.dataset.editLesson); if(lesson)lessonModal(editLesson.dataset.courseId,lesson); } catch(error) { toast(error.message,'error'); } return; }
  const deleteLesson = event.target.closest('[data-delete-lesson]');
  if (deleteLesson) { event.preventDefault(); event.stopImmediatePropagation(); confirmAction('Delete lesson?','This lesson link and its details will be removed for students.',async()=>{ try { await apiRequest(`/api/teacher/lessons/${deleteLesson.dataset.deleteLesson}`,{method:'DELETE'}); toast('Lesson deleted.'); loadTeacherLessons(deleteLesson.dataset.courseId); } catch(error) { toast(error.message,'error'); } }); return; }
  const courseDetails = event.target.closest('[data-course-details]');
  if (courseDetails) setTimeout(()=>loadStudentLessons(courseDetails.dataset.courseDetails),0);
  const logout = event.target.closest('[data-logout]');
  if (logout) { event.preventDefault(); event.stopImmediatePropagation(); await apiRequest('/api/auth/logout',{method:'POST'}).catch(()=>{}); session=null; localStorage.removeItem(SESSION_KEY); navigate('home'); return; }
  const payment = event.target.closest('[data-payment-action="approve"], [data-payment-action="reject"]');
  if (payment) { event.preventDefault(); event.stopImmediatePropagation(); const action = payment.dataset.paymentAction; if(action === 'approve'){ try { await apiRequest(`/api/teacher/payments/${payment.dataset.paymentId}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'Approved'})}); const found=db.payments.find(p=>p.id===payment.dataset.paymentId); if(found){ found.status='Approved'; } const studentId = found?.student ?? found?.student_id; const s=db.students.find(x=>x.id===studentId); if(s){ s.payment='Approved'; } save(); teacherPaymentsHydrated=false; toast('Payment approved.'); render(); } catch(error) { toast(error.message,'error'); } return; }
  if(action === 'reject'){ const reason = window.prompt('Reason for rejection'); if(reason===null) return; try { await apiRequest(`/api/teacher/payments/${payment.dataset.paymentId}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'Rejected',reason})}); const found=db.payments.find(p=>p.id===payment.dataset.paymentId); if(found){ found.status='Rejected'; found.reason=reason; } const studentId = found?.student ?? found?.student_id; const s=db.students.find(x=>x.id===studentId); if(s){ s.payment='Rejected'; } save(); teacherPaymentsHydrated=false; toast('Payment rejected.'); render(); } catch(error) { toast(error.message,'error'); } } }

    const deleteStudent = event.target.closest('[data-delete-student]');
    if (deleteStudent) { event.preventDefault(); event.stopImmediatePropagation(); const studentId = deleteStudent.dataset.deleteStudent; confirmAction('Delete this student?','This removes the student account and all related records.', async () => { try { await apiRequest(`/api/teacher/students/${studentId}`, { method: 'DELETE' }); db.students = db.students.filter(s => s.id !== studentId); save(); toast('Student deleted.'); render(); } catch (error) { toast(error.message, 'error'); } }); return; }

    const deleteCourse = event.target.closest('[data-delete-course]');
    if (deleteCourse) { event.preventDefault(); event.stopImmediatePropagation(); const courseId = deleteCourse.dataset.deleteCourse; confirmAction('Delete this course?','This removes the course and its linked lessons/exams from the platform.', async () => { try { await apiRequest(`/api/teacher/courses/${courseId}`, { method: 'DELETE' }); db.courses = db.courses.filter(c => c.id !== courseId); save(); toast('Course deleted.'); render(); } catch (error) { toast(error.message, 'error'); } }); return; }

    const deleteAnnouncement = event.target.closest('[data-delete-announcement]');
    if (deleteAnnouncement) { event.preventDefault(); event.stopImmediatePropagation(); const announcementId = deleteAnnouncement.dataset.deleteAnnouncement; confirmAction('Delete this announcement?','This announcement will be removed from the teacher dashboard and student view.', async () => { try { await apiRequest(`/api/teacher/announcements/${announcementId}`, { method: 'DELETE' }); db.announcements = db.announcements.filter(a => a.id !== announcementId); save(); toast('Announcement deleted.'); render(); } catch (error) { toast(error.message, 'error'); } }); return; }
}, true);

window.addEventListener('hashchange',render);
apiRequest('/api/courses').then(result => { db.courses = result.courses; save(); }).catch(() => {}).finally(render);